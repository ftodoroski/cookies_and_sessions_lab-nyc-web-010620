<ul>
    <% cart.each do |item| %>
        <%= item %><br>
    <% end %>
</ul>


<%= form_tag add_product_path do %>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>

    <label>Add a product to your cart</label>
    <%= text_field_tag :product %>

    <%= submit_tag 'add to cart' %>
<% end %>
